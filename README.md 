
| Keyword     | Meaning                                   | Example Use                   |
|-------------|--------------------------------------------|-------------------------------|
| `public`    | Accessible from outside the class          | Interface methods             |
| `private`   | Hidden from outside, only accessible within|
|             | the class                                  | Internal data                 |
| `protected` | Accessible within the class and subclasses | Used in inheritance           |
| `const`     | Marks data or functions as unchangeable    | Read-only data, const methods |
| `static`    | Shared by all instances or retains value across calls | Counters, class-level data |


| Ключевое слово | Значение                                             | Пример использования                 |
|----------------|----------------------------------------------------- |--------------------------------------|
| `public`       | Доступно извне класса                                | Методы интерфейса                    |
| `private`      | Скрыто, доступно только внутри класса                | Внутренние данные                    |
| `protected`    | Доступно внутри класса и его наследников             | Используется при наследовании        |
| `const`        | Обозначает неизменяемость данных или функций         | Только для чтения, const-методы      |
| `static`       | Общие для всех экз-в, сохраняют знач. между вызовами | Счётчики, данные класса      |


| Keyword     | Meaning, Access                | Example Use             |
|-------------|--------------------------------|-------------------------|
| `public`    | Accessible anywhere            | Methods for users       |
| `private`   | Only inside the class          | Internal variables      |
| `protected` | Inside class and subclasses    | Inheritance-related     |
| `const`     | Can't be changed               | Read-only values        |
| `static`    | Shared or persistent           | Common counters, class-level data |

Вот простой пример использования staticпеременных как данных уровня класса — то есть переменные, общие для всех объектов класса:

📌 Пример: class-level data - счётчик объектов

#include <iostream>

class MyClass {
private:
    static int objectCount; // Статическая переменная (class-level)
	
public:
    MyClass() {
        objectCount++; // Увеличиваем при создании объекта
    }

    static int getObjectCount() {
        return objectCount; // Доступ к статической переменной
    }
};

// Определение статической переменной вне класса
int MyClass::objectCount = 0;

int main() {
    MyClass a;
    MyClass b;
    MyClass c;

    std::cout << "Total objects created: " << MyClass::getObjectCount() << std::endl;
    return 0;
}
🧠 Объяснение:
objectCount— это статическая переменная , она принадлежит классу , а не конкретному объекту.

Каждый раз, когда создается объект MyClass, objectCountувеличивается.

Вызов MyClass::getObjectCount()покажет общее количество созданных объектов.
